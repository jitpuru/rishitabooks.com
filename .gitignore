# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editor / tooling
*.swp
.vscode/
.idea/

# Local scratch
*.log

# Held back until the DNS cutover — see notes below.
# GitHub Pages reads CNAME as "serve this site at www.rishitabooks.com",
# which would redirect the preview to the old Squarespace site.
# Delete this line when we move DNS.
CNAME
